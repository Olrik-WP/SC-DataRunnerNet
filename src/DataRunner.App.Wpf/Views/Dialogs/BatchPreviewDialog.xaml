<Window x:Class="DataRunner.App.Views.Dialogs.BatchPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:DataRunner.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=vm:BatchPreviewViewModel}"
        mc:Ignorable="d"
        Title="Send batch — preview"
        Width="900" Height="640"
        MinWidth="640" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Send batch — preview"
                       FontSize="22" FontWeight="SemiBold" />
            <TextBlock Margin="0,4,0,0" Opacity="0.85" FontSize="12" TextWrapping="Wrap"
                       Text="Each validated screenshot is sent in its own POST so UEX gets the matching visual evidence. The smart-split below redistributes commodities so no (terminal, commodity) pair is sent twice within UEX's 5-min window." />
        </StackPanel>

        <!-- EXPLAINER BAND — quick legend of the Reason column. -->
        <Border Grid.Row="1" Margin="0,0,0,10" Padding="10,7" CornerRadius="4"
                Background="{DynamicResource SubtleFillColorSecondaryBrush}">
            <DockPanel>
                <ui:SymbolIcon Symbol="Info20" Margin="0,0,8,0" Opacity="0.75" />
                <TextBlock TextWrapping="Wrap" FontSize="11" Opacity="0.85">
                    <Run Text="Kept" Foreground="#A5D6A7" FontWeight="SemiBold" /><Run Text=" rows are sent on the screenshot they were captured on. " />
                    <Run Text="Removed" Foreground="#EF9A9A" FontWeight="SemiBold" /><Run Text=" rows are skipped because a more recent screenshot of the same terminal already carries them — UEX would otherwise reject the older one with " /><Run Text="duplicated_report" FontFamily="Cascadia Mono, Consolas, monospace" /><Run Text="." />
                </TextBlock>
            </DockPanel>
        </Border>

        <!-- BODY: one row per commodity occurrence. -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="4">
            <DataGrid ItemsSource="{Binding Rows}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      RowHeight="32"
                      IsReadOnly="True"
                      Background="Transparent" BorderThickness="0"
                      AlternatingRowBackground="#08FFFFFF">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsKept}" Value="False">
                                <Setter Property="Background" Value="#22C8504F" />
                                <Setter Property="ToolTip" Value="This commodity will NOT be sent on the screenshot it was captured on. See the Reason column for details." />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Commodity" Width="2*" MinWidth="160"
                                        Binding="{Binding CommodityLabel}" />
                    <DataGridTextColumn Header="Terminal" Width="2*" MinWidth="180"
                                        Binding="{Binding TerminalLabel}" />
                    <DataGridTextColumn Header="Found on" Width="80" MinWidth="70"
                                        Binding="{Binding FoundOnLabel}" />
                    <DataGridTextColumn Header="Sent on" Width="80" MinWidth="70"
                                        Binding="{Binding SentOnLabel}" />
                    <DataGridTextColumn Header="Reason" Width="3*" MinWidth="180"
                                        Binding="{Binding Reason}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- FOOTER: stats + Cancel / Send. -->
        <Border Grid.Row="3" Margin="0,16,0,0" Padding="0,12,0,0"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ui:Button Padding="16,7" Margin="0,0,8,0"
                               Appearance="Secondary"
                               Click="OnCancelClick"
                               Content="Cancel" />
                    <ui:Button Padding="22,7"
                               Appearance="Primary"
                               Click="OnSendClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Send all" FontWeight="SemiBold" />
                            <ui:SymbolIcon Symbol="Send24" Margin="8,0,0,0" />
                        </StackPanel>
                    </ui:Button>
                </StackPanel>
                <TextBlock Text="{Binding Summary}" VerticalAlignment="Center"
                           FontSize="12" Opacity="0.85" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
